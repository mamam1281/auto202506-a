import SlotMachine from '@/components/SlotMachine'; // Adjusted import path for @ alias

export default function SlotsPage() {
  return (
    <div>
      <h1>Slots Game</h1>
      <SlotMachine />
    </div>
  );
}
